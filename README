26745130
https://github.com/totto-dev/kadai4

================================================================
課題4：最大公約数を計算するシェルスクリプトとGitHub Actionsによる自動テスト
================================================================

伏見 知子 (26745130)


【1. 概要】
本リポジトリは、2つの自然数の最大公約数(GCD)を計算するシェルスクリプトと、
その動作を自動で検証するテスト用シェルスクリプトから構成される。
GitHub Actions を用いて、リポジトリへ push するたびにテストが自動実行され、
スクリプトの正しさが継続的に確認される仕組みを構築した。


【2. ファイル構成】
~/kadai4/
├── README                      本ファイル
├── gcd.sh                      最大公約数を計算するスクリプト
├── test_gcd.sh                 gcd.sh の動作を検証するテストスクリプト
└── .github/
    └── workflows/
        └── test.yml            GitHub Actions の設定ファイル（自動テスト定義）


【3. 各ファイルの説明】

■ gcd.sh （最大公約数スクリプト）
  2つの自然数を引数として受け取り、最大公約数を標準出力に表示する。
  正しくない入力に対しては、標準エラー出力へエラーメッセージを表示し、
  終了ステータス1で終了する。

  [使用方法]
    ./gcd.sh <自然数1> <自然数2>
    例： ./gcd.sh 12 18   →  6

  [計算アルゴリズム]
    ユークリッドの互除法を採用。
    a を b で割った余りを求め、b が 0 になるまで繰り返す。
    例: gcd(18,12) → gcd(12,6) → gcd(6,0) → 答え 6

  [入力チェック（エラー終了する条件）]
    - 引数の個数が2個でない（多い・少ない・なし）
    - 自然数(1以上の整数)でない（負の数、小数、文字列、空文字、0）
    正規表現 ^[0-9]+$ で「数字のみ」を判定し、不正な入力をまとめて弾く。

  [使用しているシェルスクリプトの要素]
    - 制御構文：if 文、for ループ、while ループ、case は不使用
    - 関数定義：error_exit（エラー終了処理を共通化）
    - 数値演算：$(( a % b )) による剰余計算
    - 正規表現による入力検証：[[ "$value" =~ ^[0-9]+$ ]]
    - 標準エラー出力へのリダイレクト：>&2
    - 終了ステータスの使い分け：正常 exit 0 / 異常 exit 1


■ test_gcd.sh （テストスクリプト）
  gcd.sh にさまざまな入力を与え、想定どおりの挙動をするか自動で検証する。
  1件でも想定と異なればエラー終了(exit 1)し、全件成功なら正常終了(exit 0)する。

  [テスト内容（全24パターン）]
    ・正常系 11件：正しい最大公約数を返すか
      例) gcd(2,4)=2, gcd(12,18)=6, gcd(17,5)=1（互いに素）,
          gcd(7,7)=7, gcd(123456789,987654321)=9（大きな数）など
    ・異常系 13件：エラー終了すべき入力で正しく異常終了するか
      例) 引数が多い/少ない/なし、負の数、小数、文字列、空文字、0 など

  [判定の仕組み]
    - assert_success 関数：出力値と終了ステータス(0)の両方を検証
    - assert_error   関数：終了ステータスが0以外（異常終了）であることを検証

  [使用しているシェルスクリプトの要素]
    - 関数定義（assert_success / assert_error）
    - コマンド置換による出力取得：actual=$("$GCD" "$a" "$b")
    - 終了ステータスの取得：$?
    - カウンタ変数による集計と、最終結果に応じた exit


■ .github/workflows/test.yml （GitHub Actions 設定）
  push / pull request のたびに、GitHubのUbuntu環境上で test_gcd.sh を
  自動実行する。test_gcd.sh が exit 1 で終わるとワークフローが失敗として
  扱われ、GitHub上で通知される。

  [処理の流れ]
    1. actions/checkout でリポジトリを取得
    2. chmod +x でスクリプトに実行権限を付与
    3. ./test_gcd.sh を実行（テスト本体）


【4. 動作確認方法】
  ■ サーバ（ローカル）での確認
    $ ./test_gcd.sh
    → 全24件 [OK] と表示され、「全テスト成功」で終了する。

  ■ GitHub Actions での確認
    リポジトリの「Actions」タブから実行結果を確認できる。
    緑のチェックマークが付いていればテスト成功。


【5. 工夫した点】

(1) 正常終了とエラー終了の明確な使い分け（課題ヒント3に対応）
    エラー時は必ず標準エラー出力(>&2)へメッセージを出し、exit 1 で終了する。
    これにより GitHub Actions が失敗を正しく検知できる。

(2) エラーになりそうなパターンを網羅（課題ヒント2に対応）
    引数の過不足、負の数、小数、文字列、空文字、0 など、想定される不正入力を
    13パターン用意し、すべてで正しくエラー終了することを確認している。

(3) テストの自動化と継続的な検証
    GitHub Actions により、push のたびにテストが自動で走る。
    人手によるテスト忘れを防ぎ、スクリプトの正しさを継続的に保証する。

(4) コードの可読性
    各処理にコメントを付し、関数化により重複を排除した。


【6. 今後の拡張可能性】
- 3つ以上の数の最大公約数への対応
- 最小公倍数(LCM)の計算機能の追加
- テスト結果をファイルやログに出力する機能


【7. 参考文献】
[1] ユークリッドの互除法（アルゴリズムの確認）
    https://ja.wikipedia.org/wiki/ユークリッドの互除法

[2] GitHub Actions のワークフロー構文
    https://docs.github.com/ja/actions/using-workflows/workflow-syntax-for-github-actions

================================================================
